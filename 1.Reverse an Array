class Solution {
  public:
    void reverseArray(vector<int> &arr) {
        int n= arr.size();
        vector<int>ans;
        for(int i=n-1;i>=0;i--){
            ans.push_back(arr[i]);
        }
        for(int i=0;i<n;i++){
            arr[i]=ans[i];
        }
    }
};
